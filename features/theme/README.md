# Theme

Theme preference and persistence capability.

Composes `design-system/providers` theme primitives into application UI:

- `ThemeScript` — blocking FOUC prevention for the root layout
- `ThemeToggle` — cycles system → light → dark

Both light and dark themes are first-class.
