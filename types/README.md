# Types

Shared TypeScript contracts mirroring the content model and public APIs.

## Responsibility

Domain type definitions only. No runtime logic, no framework imports.

## Dependency rule

`types/` must not import from any other project directory.
